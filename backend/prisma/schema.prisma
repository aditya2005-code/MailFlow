# Prisma schema — to be defined in Step 1.2 (database schema design)
# generator client {
#   provider = "prisma-client-js"
# }
#
# datasource db {
#   provider = "postgresql"
#   url      = env("DATABASE_URL")
# }
